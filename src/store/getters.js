const getters = {
    headerRouters: state => state.headerRouters,
    addRoutes: state => state.addRoutes,
    user: state => state.user,
    token: state => state.token,
    userInfo: state => state.userInfo,
    menuList: state => state.menuList,
}
export default getters;